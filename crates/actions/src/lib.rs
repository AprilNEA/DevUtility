//! Global actions for DevUtility.
//!
//! Declared with `gpui::actions!` and registered on the `Workspace` entity
//! during app startup. Consumed by the macOS menu bar, the command palette,
//! and the keymap JSON.
//!
//! Per-tool actions live in their respective `tools/*_tool` crate, not here.
