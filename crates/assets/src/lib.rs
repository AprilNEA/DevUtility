//! Embedded asset bundle for DevUtility.
//!
//! Mirrors `zed/crates/assets`: a `RustEmbed`-backed asset source that
//! serves fonts, SVG icons, palette JSON, and keymap JSON compiled into
//! the binary. Intended to be registered on `Application::new().with_assets(Assets)`.
